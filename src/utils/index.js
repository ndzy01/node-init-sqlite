const formatData = require('./formatData')
const createDir = require('./createDir')
const getRandomCode = require('./randomCode')

const utils = {
  formatData,
  createDir,
  getRandomCode,
}
module.exports = utils
